"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const Rule_1 = require("../Rule");
class FindLogFiles extends Rule_1.default {
    run() {
        return __awaiter(this, void 0, void 0, function* () {
            // Look for physical log files
            yield this.getGlobbedFiles('$OUTDIR/$JOB.@(log|*lg)');
            return true;
        });
    }
}
FindLogFiles.commands = new Set(['build', 'log']);
FindLogFiles.phases = new Set(['initialize']);
FindLogFiles.alwaysEvaluate = true;
FindLogFiles.description = 'Find preexisting log files.';
exports.default = FindLogFiles;
