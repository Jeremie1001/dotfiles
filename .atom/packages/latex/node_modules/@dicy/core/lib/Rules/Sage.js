"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Rule_1 = require("../Rule");
class Sage extends Rule_1.default {
    constructCommand() {
        // Sage doesn't seem to have any logs, so try to guess at the outputs.
        return {
            args: ['sage', '{{$BASE_0}}'],
            cd: '$ROOTDIR_0',
            severity: 'error',
            outputs: [
                { file: '$DIR_0/$NAME_0.sout' },
                { file: '$DIR_0/$NAME_0.sage.cmd' },
                { file: '$DIR_0/$NAME_0.scmd' },
                { file: '$FILEPATH_0.py' }
            ],
            globbedInputs: [{ file: '$DIR_0/sage-plots-for-$JOB.tex/*' }]
        };
    }
}
Sage.parameterTypes = [new Set(['Sage'])];
Sage.description = 'Supports SageTeX by running Sage when needed.';
exports.default = Sage;
