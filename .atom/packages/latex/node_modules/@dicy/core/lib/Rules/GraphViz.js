"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Rule_1 = require("../Rule");
class GraphViz extends Rule_1.default {
    constructCommand() {
        // Right now we only call fdp. Long usefulness of this is unknown.
        return {
            args: [
                'fdp',
                `-T${this.options.outputFormat}`,
                '-o',
                '{{$DIR_0/$NAME_0$OUTEXT}}',
                '{{$FILEPATH_0}}'
            ],
            cd: '$ROOTDIR',
            severity: 'error',
            outputs: [{ file: '$DIR_0/$NAME_0.$OUTEXT' }]
        };
    }
}
GraphViz.parameterTypes = [new Set(['GraphViz'])];
GraphViz.commands = new Set(['graph']);
GraphViz.description = 'Runs GraphViz on dependency graphs.';
exports.default = GraphViz;
